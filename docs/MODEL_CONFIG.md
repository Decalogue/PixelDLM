# 模型配置说明

## 🎯 模型变体

### Base 模型

| 模型 | patch_size | 适合图像尺寸 | num_patches (64×64) | num_patches (256×256) |
|------|------------|--------------|---------------------|----------------------|
| **JiT-B/4** | 4 | 64×64 | 256 | 4096 |
| JiT-B/8 | 8 | 64×64, 128×128 | 64 | 1024 |
| JiT-B/16 | 16 | 256×256 | 16 | 256 |
| JiT-B/32 | 32 | 512×512 | 4 | 64 |

### Large 模型

| 模型 | patch_size | embed_dim | depth | num_heads |
|------|------------|-----------|-------|-----------|
| JiT-L/4 | 4 | 1024 | 24 | 16 |
| JiT-L/16 | 16 | 1024 | 24 | 16 |
| JiT-L/32 | 32 | 1024 | 24 | 16 |

### Huge 模型

| 模型 | patch_size | embed_dim | depth | num_heads |
|------|------------|-----------|-------|-----------|
| JiT-H/4 | 4 | 1280 | 32 | 16 |
| JiT-H/16 | 16 | 1280 | 32 | 16 |
| JiT-H/32 | 32 | 1280 | 32 | 16 |

---

## 📊 64×64 图像配置对比

### patch_size=4 (JiT-B/4) ✅ 推荐

```
图像尺寸: 64×64
patch_size: 4
num_patches: (64/4)^2 = 256
patch_dim: 4×4×3 = 48
序列长度: 256 tokens
```

**优势**：
- ✅ 保留更多细节（256 patches）
- ✅ 适合小图像
- ✅ 序列长度合理

### patch_size=8 (JiT-B/8)

```
图像尺寸: 64×64
patch_size: 8
num_patches: (64/8)^2 = 64
patch_dim: 8×8×3 = 192
序列长度: 64 tokens
```

**特点**：
- 中等细节保留
- 序列长度较短

### patch_size=16 (JiT-B/16) ❌ 不推荐用于 64×64

```
图像尺寸: 64×64
patch_size: 16
num_patches: (64/16)^2 = 16
patch_dim: 16×16×3 = 768
序列长度: 16 tokens
```

**问题**：
- ❌ 序列长度太短（只有 16 tokens）
- ❌ 细节丢失严重
- ❌ 不适合小图像

---

## 🎯 推荐配置

### 64×64 图像

**推荐**: `JiT-B/4`
- patch_size=4
- num_patches=256
- 保留更多细节
- 序列长度合理

### 256×256 图像

**推荐**: `JiT-B/16`
- patch_size=16
- num_patches=256
- 与 64×64 使用 JiT-B/4 的序列长度相同
- 适合大图像

---

## 📝 使用示例

### 训练 64×64 模型

```bash
python train.py \
    --model JiT-B/4 \
    --img_size 64 \
    --data_path ./data/train
```

### 训练 256×256 模型

```bash
python train.py \
    --model JiT-B/16 \
    --img_size 256 \
    --data_path ./data/train
```

---

## ✅ 总结

- ✅ **64×64 图像**: 使用 `JiT-B/4` (patch_size=4, 256 patches)
- ✅ **256×256 图像**: 使用 `JiT-B/16` (patch_size=16, 256 patches)
- ✅ 默认配置已更新为 `JiT-B/4`
- ✅ 所有相关代码已更新

---

**修改日期**: 2024-12-15
**版本**: v2.0




